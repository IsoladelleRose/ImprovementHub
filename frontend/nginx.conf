server {
    listen 80;
    server_name localhost;

    # Serve Angular app
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # For now, let Angular handle API calls directly
    # You can configure the backend URL in Angular environment files
}