{
  "name": "improvement-hub-backend",
  "version": "1.0.0",
  "description": "Java Spring Boot app deployed via Node.js",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Building Java app...' && chmod +x build.sh && ./build.sh && echo 'Installing Java for runtime...' && apt-get install -y temurin-21-jdk"
  },
  "dependencies": {
    "child_process": "^1.0.2"
  },
  "engines": {
    "node": "18.x"
  }
}